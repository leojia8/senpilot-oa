# Gemini
GEMINI_API_KEY=your-gemini-api-key

# Gmail OAuth (files, not committed — see README)
GMAIL_CREDENTIALS_FILE=credentials.json
GMAIL_TOKEN_FILE=token.json

# Agent behaviour
POLL_INTERVAL_SECONDS=60
MAX_DOCUMENTS=10
HEADLESS=false
